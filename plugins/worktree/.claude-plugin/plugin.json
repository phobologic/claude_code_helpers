{
  "name": "claude-worktree",
  "version": "1.2.0",
  "description": "Git worktree management: symlink shared state (e.g. .tickets/) across worktrees via .worktreelinks, copy per-worktree files via .worktreeinclude. Includes PreToolUse guard that pre-creates worktrees for team agents (workaround for anthropics/claude-code#33045)."
}
